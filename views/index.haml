#home.front-container.page
  .front-card
    -# .span6.columns
    #home-carousel.carousel.slide
      .carousel-inner
        = each_content_item "home-carousel-images" do |i|
          .item
            %img{:src=>s3_link("/images/#{i}")}
            .carousel-caption
              - name = i.rpartition(".")[0]
              = content "#{name}"
              -# %h4 Welcome to the Conversation!
              -# %p We aspire to create a safe, friendly place in which real people can discuss whatever they find apropos, impactful, interesting etc.

      -# %a(class="left carousel-control" href="#home-carousel" data-slide="prev") &lsaquo;
      -# %a(class="right carousel-control" href="#home-carousel" data-slide="next") &rsaquo;
    
  -# .row
  -#   .span4
  -#     = content "home-bottom-left"
  -#   .span4
  -#     = content "home-bottom-middle"
  -#   .span4
  -#     =content "home-bottom-right"

#about.hero-unit.page
  .row
    .span12
      = content "about"

-# = each_content_item "pages" do |i|
-#   %div.feature{:id => "page-#{i.downcase}" }
-#     .page
-#       = content "/pages/#{i}"


-# #cross-you.feature
-#   .page
-#     <h1>Coming Soon</h1>
-#     <h1>A place to find engaging conversations on politics, faith, art, Dr. Who, etc. All the important stuff in life</h1>
-#     <h1>But for now we just have the <a href="https://www.facebook.com/groups/Helt55/">facebook forum</a>, and the below preview of recent posts:</h1>
-#   .forum

-# #notifications
-#   %h1#h1-home Home
-#   = each_content_item "pages" do |i|
-#     %h1{:id => "h1-#{i.downcase}" } #{i}



-# #post-template(style="display:none")
-#   {{#data}}
-#   .post.span-18
-#     .row
-#       %h1.prepend-2 {{name}}
-#       {{#picture}}
-#       %a.span-4.prepend-2.image(href='#')
-#         %img(src='{{picture}}')
-#       .span-13.last
-#         {{#from}}
-#         %h2 {{from.name}}
-#         {{/from}}
-#         {{description}}
-#       {{/picture}}
-#   {{/data}}

-# {{^picture}}
-#   .span-17.last
-#     {{#from}}
-#     %h2 {{from.name}}
-#     {{/from}}
-#     {{description}}
-#   {{/picture}}
